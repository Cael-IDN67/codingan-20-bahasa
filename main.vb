Module M
 Sub Main()
  Dim p As Integer = Console.ReadLine()
  If p=1 Then
   Dim a As Double = Console.ReadLine()
   Dim t As Double = Console.ReadLine()
   Console.WriteLine(0.5*a*t)
  Else
   Dim x As Double = Console.ReadLine()
   Dim y As Double = Console.ReadLine()
   Console.WriteLine(x*y)
  End If
 End Sub
End Module
